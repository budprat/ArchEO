# OpenEO FastAPI Requirements
openeo-fastapi>=2025.5.1

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# Validation
jsonschema>=4.20.0

# Scientific computing (for uncertainty quantification)
scipy>=1.11.0

# Claude Agent SDK (installed with --no-deps; mcp shim in venv provides stubs)
# Note: Do NOT pip install mcp - it requires pydantic>=2 which conflicts with openeo-fastapi
claude-agent-sdk>=0.1.31
