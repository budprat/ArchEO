{
  "name": "change_default",
  "task": "change_detection",
  "version": "1.0.0",
  "description": "Default change detection model for bi-temporal analysis using Siamese U-Net",
  "input_bands": ["red", "green", "blue", "nir"],
  "input_size": 256,
  "output_classes": ["no_change", "change"],
  "architecture": "siamese_unet",
  "framework": "onnx",
  "model_file": "model.onnx",
  "preprocessing": {
    "normalize": true,
    "stack_mode": "concatenate"
  },
  "training": {
    "dataset": "LEVIR-CD-like",
    "samples": 5000,
    "epochs": 100
  },
  "notes": "Requires two input images (before/after). Place model.onnx in this directory for real inference."
}
